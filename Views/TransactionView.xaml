<UserControl x:Class="wpf_projekt.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">

    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Rok" FontWeight="Bold"/>
                <ComboBox Width="80"
                          ItemsSource="{Binding AvailableYears}"
                          SelectedItem="{Binding SelectedYear}"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Miesiąc" FontWeight="Bold"/>
                <ComboBox Width="100"
                          ItemsSource="{Binding AvailableMonths}"
                          SelectedItem="{Binding SelectedMonth}"
                          DisplayMemberPath="Label"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Kategoria" FontWeight="Bold"/>
                <ComboBox Width="130"
                          ItemsSource="{Binding AvailableCategories}"
                          SelectedItem="{Binding SelectedCategory}"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Rodzaj" FontWeight="Bold"/>
                <ComboBox Width="100"
                          SelectedItem="{Binding SelectedType}">
                    <ComboBox.Items>
                        <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Wszystkie</system:String>
                        <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Wydatek</system:String>
                        <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Przychód</system:String>
                    </ComboBox.Items>
                </ComboBox>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Data" FontWeight="Bold"/>
                <ComboBox Width="120"
                          SelectedItem="{Binding SelectedSort}">
                    <ComboBox.Items>
                        <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Od najnowszej</system:String>
                        <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Od najstarszej</system:String>
                    </ComboBox.Items>
                </ComboBox>
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Margin="20,0,0,0">
                <Button Content="Eksportuj do CSV"
                        Width="120" Height="22"
                        Command="{Binding ExportToCsvCommand}"
                        ToolTip="Zapisz widoczne dane do pliku Excel/CSV"
                        Cursor="Hand"/>
            </StackPanel>

        </StackPanel>

        <DataGrid ItemsSource="{Binding FilteredTransactions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Height="400"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data"
                                    Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}"
                                    Width="100"/>
                <DataGridTextColumn Header="Kategoria"
                                    Binding="{Binding TransactionType.Name}"
                                    Width="150"/>
                <DataGridTextColumn Header="Kwota"
                                    Binding="{Binding Amount}"
                                    Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPositive}" Value="True">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsPositive}" Value="False">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Typ"
                                    Binding="{Binding TypeName}"
                                    Width="80"/>
                <DataGridTextColumn Header="Opis"
                                    Binding="{Binding Description}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
